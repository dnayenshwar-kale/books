spring:
#  profiles:
#    active: dev
  datasource:
    url: jdbc:h2:file:./src/main/resources/h2/database/bookdb
    driverClassName: org.h2.Driver
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: update

management:
  endpoint:
    health:
      show-details: always
